{
  "name": "poe2-build-analyzer-v2",
  "private": true,
  "version": "0.1.0",
  "description": "Path of Exile 2 build analyzer — one shared analysis core, consumed by an MCP server and a web app.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspace @poe2/core && npm run build --workspace @poe2/web",
    "test": "npm run test --workspace @poe2/core",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev": "npm run dev --workspace @poe2/web"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
